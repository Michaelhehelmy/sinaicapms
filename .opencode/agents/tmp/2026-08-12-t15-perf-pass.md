---
task_id: t15-perf-pass
parent_task: Implement all remaining backlog (T15 performance pass)
created: 2026-08-12
status: done
category: frontend
---

# Tmp Agent: T15 — Performance pass (Lighthouse budgets + hydration tuning + caching audit)

## Objective
Add a Lighthouse performance-budget baseline, tune hydration directives on public pages (`client:visible`/`client:idle` for below-fold components instead of eager loads), and audit the backend caching surface — without regressing any test.

## Scope
- Files to touch:
  - NEW `lighthouserc.cjs` (or `budget.json` referenced from it) at repo root or `app/` root: budgets for performance (LCP < 2.5s, TBT < 200ms, CLS < 0.1, FCP < 1.8s) and a `lighthouse` npm script wired into `app/package.json` scripts (only if the script requires no new dependency — use `npx lighthouse` invocation; do NOT install new deps).
  - `app/src/components/public/**` + `app/src/pages/*.astro`: audit `client:` directives on React islands (TenantLanding, MarketplaceHome, CampBooking, contact form). Change eager hydration (`client:load`/default) to `client:visible` or `client:idle` ONLY where the component is below the fold or non-interactive-critical. Keep `client:load` on above-the-fold interactive components.
  - Read-only audit (report findings in your final message, do not change unless trivial+safe): `backend/src/utils/response.js` `cachedJsonResponse` usage — note which endpoints use it, whether `Cache-Control` is set, and whether KV caching is safe under the 1,000-writes/day free-plan limit. Do NOT modify backend behavior in this task (rate-limit KV is intentionally disabled).
- Must NOT touch: `app/astro.config.mjs` (T12 owns image config), tests/e2e baselines, `backend/wrangler.toml`.

## Done Condition
- `lighthouserc.cjs`/budget file exists and `app/package.json` has a `"lighthouse"` script.
- Grep shows below-fold islands using `client:visible`/`client:idle` where appropriate.
- `cd app && npx vitest run` passes and `cd app && npm run build` passes.

## Steps
1. Inventory `client:` directives in public components/pages.
2. Write budgets file + script.
3. Adjust hydration directives conservatively (verify each change doesn't hide interactive UI needed by E2E).
4. Audit backend caching; write findings summary.
5. Build + vitest.
6. Set `status: done` in this file's frontmatter.
