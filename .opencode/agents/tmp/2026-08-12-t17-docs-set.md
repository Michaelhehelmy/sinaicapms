---
task_id: t17-docs-set
parent_task: Implement all remaining backlog (T17 documentation set)
created: 2026-08-12
status: done
category: docs
---

# Tmp Agent: T17 — Documentation set + minimal example

## Objective
Create the missing `docs/` documentation set (7 files) plus an `examples/minimal/` reference, describing the CURRENT state of the codebase (verify against code, not AGENTS.md claims — the codebase has drifted: no i18n system, hard-coded English LTR, TanStack Query admin, sharp-ready image config after T12).

## Scope
- Files to create:
  1. `docs/ARCHITECTURE.md` — four-layer contract (Frontend → API → D1/KV), zone model (`routeZones.ts`), Hono worker layout, migration strategy.
  2. `docs/API_CONTRACT.md` — `app/src/lib/api.ts` as the contract; auth model (JWT, admin vs pos_token), key endpoints, response envelope; reference the generated OpenAPI at `/api/openapi.json` and `gen:openapi`/`gen:types` scripts.
  3. `docs/COMPONENT_CATALOG.md` — list `app/src/components/ui/` (all ~25 after T9) with a11y notes; admin panels, pos views, public components, hooks (`useAdminData`, `useQueryHooks` etc.).
  4. `docs/DEVELOPER_ROADMAP.md` — the backlog state: done items (T8 openapi, T12 image pipeline, T14 pos terminal, T16 a11y suite, T18 docs), cancelled (T11 RTL — hard-coded English LTR decision), remaining/partial (T9 expansion, T10 SEO, T13 query migration, T15 perf) with current status.
  5. `docs/MIGRATION_GUIDE.md` — how D1 migrations are created/applied (see `.opencode/skills/database/db-migration`), numbering (0053 latest: camp ownership), the `pos_users.name` generated column + `organization_id NOT NULL` gotchas, KV free-plan rate-limit guidance.
  6. `docs/QUICK_START.md` — clone, `npm install` (root, app, backend), env vars (JWT_SECRET), `wrangler dev`, `astro dev`, test commands (unit + E2E with `CI=true npx playwright test`), `./deploy.sh` (+ `--staging`).
  7. `docs/TESTING.md` — suites and counts (backend 1082, app 1465, root 169, E2E 566 total/552 gate), CI mode notes, port hygiene before full E2E (`ss -tlnp | grep -E '4320|8787'`), `.last-run.json` ground truth.
  - `examples/minimal/README.md` — 30-line minimal marketplace fetch snippet + note pointing at `examples/` intent.
- Must NOT touch: `app/**`, `backend/**`, tests, `AGENTS.md`/`AGENT_LOGBOOK.md` (logbook updates are orchestrated separately).

## Done Condition
- All 7 docs + example exist and are internally consistent (verify file existence).
- Content reflects the codebase: run `ls app/src/components/ui/ | wc -l`, check `app/src/hooks/`, verify no i18n dir exists, verify T12 config change (no `passthroughImageService`) before writing — adjust prose accordingly.

## Steps
1. Verify current state quickly (config, ui count, hooks, e2e spec notes).
2. Write each file with accurate, concise content.
3. Set `status: done` in this file's frontmatter.
