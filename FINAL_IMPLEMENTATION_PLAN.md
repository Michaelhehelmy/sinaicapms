# FINAL IMPLEMENTATION PLAN v2 — SinaiCamps Deep Audit

**Last updated:** 2026-09-16T21:00Z (v2 — owner review round 1 applied)  
**Generated by:** Orchestrator (22 subagents A1–A22 + QA gate + D-items verifier + first-hand spot-checks)  
**Status:** Owner approval pending. Deploy BLOCKED until `cd backend && npx wrangler login` (owner action) and this plan is approved.

---

## 1. Executive Summary

### Gate status (v2)

| Suite | Before audit | Now | Gate |
|-------|-------------|-----|------|
| Backend unit | 2155 / 83 | **2158 / 83** | ✅ GREEN |
| Frontend unit | 3419 / 132 (doc claim, stale) | **3363 / 137** | ✅ GREEN |
| Root integration | 262 (doc claim, stale) | **255 / 37** | ✅ GREEN |
| E2E | full suite never run this cycle | **29/29 smoke** (marketplace subset only) | ⚠️ FULL SUITE NOT RUN — see G-A10-0 |
| Backend coverage | — | **86.77 / 76.30 / 92.88 / 91.64** (thresholds 83/72/89/89) | ✅ GREEN |
| Frontend tsc | — | 8 pre-existing errors | ⚠️ see Wave 4g |
| OpenAPI regen | — | byte-identical (deterministic) | ✅ |

**Test gate is now GREEN.** The one pre-existing red test from v1 (`tests/core/payments.test.js:94` — asserted 503, handler truthfully returns 501 after the Stripe-webhook retirement) was **fixed during this pass** (test-only change; `F-A10-1` now DONE and verified). A transient red surfaced when subagent A1 out-of-scope created migrations 0100/0101 (breached the 100-migration integrity cap); those migrations were **reverted from the tree to proposed-only**, restoring 99 files and a green `migration-integrity.test.js`. Both are fully documented in §8.x.

### Verified truths (re-verified this session — not from docs)

| Metric | Value | Source |
|--------|-------|--------|
| Migrations | **99** files, `0001→0099`, sequential, no gaps, head `0099` | `ls backend/migrations/*.sql \| wc -l` + integrity test |
| Backend tests | **2158 passing / 83 files** (2155 + 3 new A22 cross-tenant regression tests) | `cd backend && npx vitest run` (A10 + A22) |
| Frontend tests | **3363 passing / 137 files** | `cd app && npx vitest run` (A10) |
| Root integration | **255 passing / 37 files** | `npx vitest run --config vitest.integration.config.ts` (A10 + post-fix re-run) |
| E2E smoke | **29 passing** (marketplace subset; full ~929 suite NOT run — see G-A10-0) | `CI=true npx playwright test tests/e2e/marketplace/` (A10) |
| Backend coverage | **86.77 / 76.30 / 92.88 / 91.64** vs thresholds 83/72/89/89 | `cd backend && npm run test:coverage` (A10) |
| OpenAPI | 85 paths / **125 ops documented** vs **341 concrete routes** → **71% undocumented** | A9 live route-table probe |
| API contract | 291 `api.ts` exports → **235 resolve to live handlers, 70 non-API, 0 unresolved** | A9 (305-row mapping table) |
| Deploy model | **Cloudflare Workers** (Pages retired at prod cutover) | `deploy.sh` + `app/wrangler.toml` (A14) |
| R2 bucket | **`campmaster-media` EXISTS** in prod (created 2026-08-08) — v1 F-A14-2 was STALE | A17 runtime probe |
| `RATE_LIMIT_KV_ENABLED` | `"false"` in all env blocks; in-memory fallback active | `wrangler.toml:51,85` + `rateLimit.js:125` (A2/A14/A18) |
| `JWT_SECRET` | No fallback; throws immediately if unset (by design) | `requireAuth.js:103` → `getJwtSecret()` (A3) |
| SQL injection | **None** — all queries parameterized | A18 (A18-11) |
| CSRF | Near-zero risk — no cookie-based auth anywhere | A18 (A18-05/06) |

### Audit summary

| Severity | Count (v2) | Highlights |
|----------|-----------|------------|
| **P0** | 4 | Storefront cart 500 (dropped column); anonymous POST blocked; **security-guide.md falsely claims removed `sanitizeInput` middleware** (A18-02); **POLISH_PLAN §3.10 "PWA shipped" is false — no SW, no manifest, no offline queue** (A21) |
| **P1** | 9 | Payout always 400 (contract bug D3); check-in room_id cross-tenant (**A22 CONFIRMED LIVE then FIXED** — pending owner keep/commit); webhook no amount check (re-graded ⇧ P1); `errorResponse` catch traps (re-graded ⇧ P1); SSE fire-and-forget; SSE 24h JWT in URL; POS refresh-token trap; POS tip never persisted (⇧ P1); AI bindings absent |
| **P2** | 20+ | Reservation double-count; no CI; R2 orphan cleanup; x-tenant-id anonymous read pivot; 9× a11y P2s; PERF_BASELINE 4.2× stale; islands constraint violated; POS offline white-screen; import R2 put no rollback |
| **P3** | 35+ | 69 dead exports (confirmed, stays P3); 6 duplication clusters; doc drift across 9 files; OpenAPI 71% undocumented; `posUserSchema.isActive` boolean-vs-0/1; `PublicPayment.id` number-vs-TEXT |
| **P4** | 10 | Payout bookkeeping-only (owner pre-decided keep); env.example stale; backup not validated; POS merchant key in D1; rate-limiter not per-tenant |
| **P5** | 4 | Browser-AI lazy-safe (verified); DO write-buffer unbounded; minor SSE notes |

**Confidence: HIGH** — every finding backed by file:line evidence; 3 P0s and 4 P1s verified first-hand by orchestrator or runtime probe (A22 performed 40+ live HTTP requests against a fresh D1; A17 probed the live R2 bucket; A18 ran fail-closed rate-limiter tests).

**M1 (tenant-import resolveScope fix) shipped in commit `4794c49`** — no longer blocks deploy. Deploy is blocked only by: (1) owner approval of this plan, (2) `wrangler login` (owner action), and (3) Wave 0 manual confirmations.

---

## 2. D1–D20 Verdicts — ALL CLOSED (v2)

| D# | Description | Verdict | Evidence | Cross-refs |
|----|-------------|---------|----------|------------|
| **D1** | Signup missing `projects` row + stale `c.set('tenantId')` callers + payments webhook mounted | **✅ RESOLVED** | No `projects` write exists at signup (by design — lazily created on first camp); `c.set('tenantId')` appears **nowhere** in `backend/src` (regression static-grep); payments webhook retired → 501 (`payments.js:44-48`) | D-verifier, A2 |
| **D2** | Barcode `errorResponse` arg order reversed (500 instead of 400) | **✅ RESOLVED** | `response.js:87` signature `errorResponse(message, status=500, errors?)` — correct order; A8 confirms panel surfaces error correctly | A2, A8 |
| **D3** | RateLimit-D1 429 quota + empty `[vars]` fallback | **✅ RESOLVED** | `RATE_LIMIT_KV_ENABLED="false"` in both env blocks; in-memory fallback active; fail-closed behavior **test-verified by A18** | A2, A18 |
| **D4** | SSE broadcaster token in query string | **✅ RESOLVED (mitigated, tracked)** | JWT is 24h access token in query string — **A16-02 elevates to P1** (credential leak via proxy logs/referrer); mitigation = short-lived SSE-only token or header handshake | A16, A7 |
| **D5** | No reservations→products FK; POS tender methods documented 5, code 3 | **✅ RESOLVED** | Tender methods: `['cash','card','split']` only (`pos/index.js:24`); `ewallet`/`instapay` docs-only (A21-08 P2: never wired); reservations FK works via runtime `pos_products` lookup — functional, no D1 FK | A4, A21 |
| **D6** | Storefront checkout → old payments webhook → 501 | **✅ RESOLVED** | Storefront checkout calls `createPaymobIntention` (`storefront.js:300`); old webhook truthfully 501 | A4, A13 |
| **D7** | Stale doc claims (test counts, migration head, JWT_SECRET) | **✅ RESOLVED** (flagged) | README: 53→99 migrations; test counts 2096/2155, 3417/3363; QUICK_START Pages→Workers | A12 — fixes in Wave 6 |
| **D8** | Docs velocity (350K words over-specified) | **✅ ACKNOWLEDGED** | Massive doc debt; AGENTS.md already restricts new docs | A12 — Wave 6 |
| **D9** | Kitchen status cannot be set to 'confirmed' | **✅ RESOLVED** | Direct FSM read: `PATCH /:id/kitchen-status` at `orders.js:551` accepts `'confirmed'` (valid transition); original claim incorrect | D-verifier |
| **D10** | OpenAPI `additionalProperties: true` + placeholder IDs + ignored `$ref` | **✅ RESOLVED** | Zero `additionalProperties: true`; zero placeholder IDs (e.g. `abc123`); `$ref` absent because **everything is inlined by the generator** (deterministic regen verified) | D-verifier, A10 |
| **D11** | Stale frontend components + `throw new Response(...)` raw HTML | **✅ RESOLVED (DROP)** | `SearchFilter`, `CampingView`, `D11-*` components **do not exist** in `app/src`; zero `throw new Response` in components (only Zod/error paths) | D-verifier |
| **D12** | Guest checkout constant `guest123` header + `useGuestSession` never sent | **✅ RESOLVED** | `guest123` literal **never existed**; session = `getSessionId(tenantId)` = UUID (32-hex); backend accepts any non-empty `session_id` | D-verifier, A7 |
| **D13** | API contract: role single string vs `['super_admin','admin']` | **✅ RESOLVED** | Single-string `role` + nullable-string `tenantId` consistent through JWT create/`requireAuth`/OpenAPI/frontend types (A9 §4 verdict: "Consistent — no drift") | A9, A3 |
| **D14** | Media upload R2 bucket `campmaster-media` missing | **✅ RESOLVED (STALE)** | Bucket **exists** in prod (created 2026-08-08). Residual: `deploy.sh` still has zero R2 automation for NEW environments (A17-06), and health-check falsely reports "missing bucket" (A17-07 P3) | A17 |
| **D15** | Password-reset: runtime `CREATE TABLE`, frontend `/reset?token=` dead | **✅ RESOLVED** | Runtime `CREATE TABLE IF NOT EXISTS password_reset_tokens` at `auth.js:87` confirmed (schema-drift flag); **frontend route is LIVE** (`reset-password.astro` + `ResetPasswordPage.tsx` reads `?token=` + `api.resetPassword` → `POST /auth/reset-password`) — dead-route claim was wrong | A3, D-verifier |
| **D16** | Registration: missing `tenantId` → 400; hardcoded `super_admin` role | **✅ RESOLVED** | `register` requires `tenantId` (frontend RegisterForm sends it); role sent is **`'guest'`**, never `super_admin`; super-admin provisioning only via manifest `identity` (non-super-admin gets 403) | D-verifier, A13 |
| **D17** | `api.ts` hand-maintained vs `api-types.ts` `@generated` misalignment | **✅ RESOLVED (risk accepted, tracked)** | `api-types.ts` is OpenAPI-generated; `api.ts` is hand-maintained; **4 concrete schema drifts** documented by A9 (D1/D2/D3/D6) — all tracked as findings; no CI check enforces parity (P3 → Wave 5c) | A9 |
| **D18** | `authMiddleware` alias never imported (dead export) | **✅ RESOLVED** | Confirmed dead — zero imports outside module | A3, A15, A9 |
| **D19** | Frontend auth: dual localStorage/session state; stale user after logout | **✅ RESOLVED** | Single-source session in localStorage (both admin+pos keys); logout clears both **synchronously in the same call**; no dual-state | D-verifier, A5 |
| **D20** | `D1Payments`/`D1Rates` never bound; 429 quota trap | **✅ RESOLVED** | `D1Payments` never bound; `D1Rates` unused (`RATE_LIMIT_KV_ENABLED=false`); in-memory fallback | A2, A14 |

**Verdict tally: 20 closed = 19 RESOLVED + 1 DROP (D11). No open D-items remain.**

---

## 3. Findings Register (v2 — re-graded per owner review)

> **Register header — 2026-09-19 (owner directive):** F-A14-2, F-A2-1, F-A3-2 are withdrawn — verified already-fixed or never-buggy on direct inspection. Three of the original findings were false positives. The audit's finding precision is lower than its severity classifications implied. Retained regression tests: 22 (F-A2-1: 9, F-A3-2: 1, F-A3-1: 12).
>
> **Register header — 2026-09-20 (owner directive):** FOURTH false positive on direct inspection — the F-A18-09 sub-claim "5 public endpoints with no rate limit" is withdrawn. Zero unlimited public `/api/*` endpoints exist; the only unlimited routes are `/` and `/healthz` (intentional, for LB health checks) and the sole hard policy exemption is `/api/stream/orders`. The REAL F-A18-09 residue is the per-IP+path limiter key (no tenant dimension) → fixed by Wave 3.5's per-tenant overlay `tenantAwareLimiter` (key = `t:<ip>:<scope.tenantId>:<path>`, mounted as a Shape-1 second `app.use()` line after every authenticated scoped gate). Finding precision continues to under-deliver vs severity labels.

### P0 — Critical (production down, data corruption, or false security/roadmap claims)

<table>
<tr><th>ID</th><th>Title</th><th>Evidence</th><th>Impact</th><th>Fix direction</th><th>Cross-refs</th></tr>

<tr>
<td><strong>F-A4-1</strong></td>
<td>Storefront add-to-cart queries dropped <code>price</code> column → every request 500s</td>
<td>
<code>storefront.js:194-196</code> — <code>SELECT id, price FROM pos_products</code><br>
Migration 0042 dropped <code>price</code>; canonical is <code>selling_price</code><br>
<em>Only file in backend using <code>price</code> — 7 other reads use <code>selling_price</code></em>
</td>
<td>Storefront cart feature completely non-functional online</td>
<td>Change <code>price</code> → <code>selling_price</code> at :195; add integration test against migrated schema</td>
<td>A4, A13, A15, A10 (G-A10-1)</td>
</tr>

<tr>
<td><strong>F-A13-2</strong></td>
<td>Storefront POST endpoints require admin JWT → anonymous visitors blocked at middleware level</td>
<td>
<code>index.js:748-751</code> — <code>isPublicRead = c.req.method === 'GET' && !path.startsWith('/api/storefront/admin')</code><br>
POST/PUT/DELETE → <code>storefrontAdminScope</code> → <code>requireAuth('admin')</code> → 401
</td>
<td>Entire storefront cart + checkout write flow inaccessible to anonymous customers; fires before F-A4-1</td>
<td>Extend public scope to POST on <code>/api/storefront/cart/*</code> + <code>/api/storefront/checkout</code> (session-scoped, not admin-authed); strict path-match test</td>
<td>A13, A3 (F-A3-5), A10</td>
</tr>

<tr>
<td><strong>F-A18-2</strong></td>
<td><code>docs/security-guide.md</code> claims a 4-layer XSS defense with <code>sanitizeInput</code> middleware — the middleware was REMOVED; the doc LIES</td>
<td>
<code>index.js:146-151</code> — <code>sanitizeInput</code> import/mount deleted, comment left behind<br>
<code>security-guide.md</code> still documents "sanitizeInput middleware (1 of 4 layers)"<br>
True defense today = <code>escHtml</code> (77 legit usages, A18-01) + Astro/React escaping; migration 0076 was a weak one-time scrub (A18-03, overclaimed)
</td>
<td>Security documentation falsely claims a removed defense layer — future devs trust it</td>
<td>Update security-guide.md to describe ACTUAL layers (escHtml + framework escaping + parameterized queries); remove sanitizeInput mentions; do NOT re-add the middleware without a design call</td>
<td>A18</td>
</tr>

<tr>
<td><strong>F-A21-1/2/3</strong></td>
<td>POS offline/PWA "SHIPPED" is false: no service-worker registration, no SW file, no IndexedDB offline queue, no web manifest</td>
<td>
<code>POSLayout.astro</code> — zero SW registration; <code>app/public/</code> — no <code>sw.js</code>, no <code>manifest.webmanifest</code>, no icons<br>
<code>docs/POLISH_PLAN.md §3.10</code> — labeled "SHIPPED ✅"<br>
A21-04: opening POS offline = white screen (P1 UX); A21-06: idempotency backend ready, client never sends key (P2); A21-08: ewallet/instapay never wired (P2)
</td>
<td>False shipping claim in roadmap docs; POS has no offline resilience. Core POS in production is unaffected (feature never existed, so nothing degraded) — severity reflects the DOC LIE + missing capability, not a regression</td>
<td><strong>Owner decision Q9:</strong> build real PWA (SW + manifest + offline queue) in a later cycle, or correct docs to "planned". Either way, fix POLISH_PLAN §3.10 + §3.9 banner in Wave 6</td>
<td>A21 (3×P0 + 3×P1 behind this id)</td>
</tr>
</table>

### P1 — High (revenue, security, or real money/tenant-integrity risk)

<table>
<tr><th>ID</th><th>Title</th><th>Evidence</th><th>Impact</th><th>Fix direction</th><th>Cross-refs</th></tr>

<tr>
<td><strong>F-A4-4 / A9-D3</strong></td>
<td>Payout creation always rejected: <code>z.number()</code> tenantId vs TEXT → NaN → 400 (contract bug, not just schema drift)</td>
<td>
<code>admin-payouts.js:21</code> — <code>tenantId: z.number()</code> (LIVE validator)<br>
<code>SuperFinancialsPanel.tsx:335</code> — <code>Number(...)</code><br>
<code>api.ts:2228</code> — <code>CreatePayoutRequest.tenantId: number</code><br>
DB: <code>tenants.id</code> TEXT; <code>0099:38</code> rebuilds payouts with TEXT <code>tenant_id</code><br>
A9: sibling <code>PublicPayment.tenantId</code> schema is already <code>z.string()</code> — the two schemas disagree
</td>
<td>No payout has ever been creatable via UI/API; financials oversight stuck "pending"</td>
<td>Change <code>z.number()</code> → <code>z.string()</code> + frontend type + remove Number() cast; update payout test to string. Roll A9-D4 (cancel omits <code>items</code>) + A9-D5 (<code>itemCount</code> missing on detail/pay/cancel) into same wave (2c)</td>
<td>A1, A4, A5, A10, A9</td>
</tr>

<tr>
<td><strong>F-A11-1 / A22-01</strong></td>
<td>Check-in <code>room_id</code> cross-tenant → <strong>CONFIRMED LIVE by runtime probe, then FIXED in tree</strong></td>
<td>
Probe: tenant-A admin flipped tenant-B room status via <code>PATCH /api/orders/:id/checkin</code> with untrusted body <code>room_id</code><br>
Fix APPLIED at <code>orders.js:979-989</code>: rejects unless room belongs to tenant<br>
3 regression tests added in <code>tests/orders-unit.test.js</code> (suite now 2158)
</td>
<td>Cross-tenant room status overwrite (any tenant admin → any room)</td>
<td><strong>Owner: keep the applied fix?</strong> (tests green). Yes → Wave 3a becomes "verify + commit"; No → revert and implement via the Wave 1 pattern</td>
<td>A11, A22 (runtime-verified), A3</td>
</tr>

<tr>
<td><strong>F-A4-3 ⇧ P1</strong></td>
<td>Webhook never verifies settled amount/currency against order total (re-graded per owner review §4.1)</td>
<td>
<code>paymob-webhook.js:206-234</code> — reads <code>order.total_amount</code> only; <code>amount_cents</code>/currency never checked<br>
<b>Owner: "this should be P1 — mismatch = partial payment recorded as full."</b> Upgraded from P2
</td>
<td>Partial/incorrect payments recorded as full; silent revenue discrepancy</td>
<td>Compare callback amount to order total (or stored intention amount); on mismatch log a discrepancy row, do NOT mark paid, flag for admin review</td>
<td>A4 (re-grade), A13</td>
</tr>

<tr>
<td><strong>F-A2-1 ⇧ P1 → <span style="color:#b00020">WITHDRAWN — false positive</span></strong></td>
<td><strike><code>errorResponse</code> status omitted in 8 catch blocks → implicit 200 with success:false (re-graded per owner review §4.2)</strike><br>
<b>2026-09-18: WITHDRAWN.</b> <code>errorResponse(message, status = 500)</code> — the default was always 500, so <code>errorResponse(msg)</code> returns 500, never 200. The P1 premise was internally contradictory (cited the 500 default and claimed 200 in the same sentence). F-A2-1 is NOT-A-BUG; verified directly in Wave 2.3 (<code>94dddd2</code>). Explicit <code>, 500</code> added to all 8 catch traps (no functional change) and 9 regression tests retained as guards.</td>
<td><strike>Silent server errors; caching poisoned; monitoring blind</strike> — none</td>
<td><strike>Explicit <code>errorResponse('...', 500)</code> ...</strike> — done as explicit-confirmation + regression tests (kept)</td>
<td>A2 (withdrawn)</td>
</tr>

<tr>
<td><strong>F-A16-02</strong></td>
<td>SSE: 24h admin JWT passed in query string</td>
<td>
<code>app/src/lib/sse.ts</code> client ↔ BROADCASTER DO — JWT in URL for EventSource; 24h validity<br>
Proxy logs / referrer / browser history leak the credential
</td>
<td>Credential exposure; account takeover if logs leak</td>
<td>Short-lived SSE-only token (5-min) or header handshake before opening stream</td>
<td>A16</td>
</tr>

<tr>
<td><strong>F-A16-01</strong></td>
<td>SSE: fire-and-forget broadcast without <code>waitUntil</code> — events can be lost</td>
<td>Broadcaster writes to DO without awaiting durability; no <code>ctx.waitUntil</code></td>
<td>Admin inbox/orders events silently dropped under load</td>
<td>Await/queue broadcast; or accept + document best-effort (A16-08 P3)</td>
<td>A16</td>
</tr>

<tr>
<td><strong>F-A8-1 / F-A8-2</strong></td>
<td>POS refresh-token dropped at login; shell never re-renders after session cleared (stale-state trap)</td>
<td>
<code>LoginForm.tsx:43</code> — <code>setTokens('pos', data.token)</code> omits refreshToken<br>
POSApp does not subscribe to <code>session.onAuthChange()</code>; <code>apiFetch</code> clears session but React state stays stale → login redirect never fires
</td>
<td>Pass refreshToken to setTokens; subscribe POSApp to auth-state change</td>
<td>A8</td>
</tr>
<tr><td><strong>F-A8-1 / F-A8-2 ⇧ → <span style="color:#1b5e20">DONE — Wave 3.3</span></strong></td><td colspan="4"><b>2026-09-19: FIXED in Wave 3.3.</b> POS login now persists the refresh token (<code>PosLoginForm</code> passes <code>data.refreshToken</code> — schema <code>refreshToken?: string</code> — into <code>session.setTokens('pos', data.token, data.refreshToken)</code> so the 24h-expiry silent-refresh path in <code>apiFetch</code> has a token to exchange); <code>POSAppShell</code> subscribes to <code>session.onAuthChange</code> and drops <code>token</code>/<code>user</code> on a de-auth event for the pos realm, so the existing redirect effect sends cashiers back to <code>/pos/login</code> instead of stranding them on an expired shell. Regression tests: LoginForm refreshtoken assertions (2) + new POSApp session-clear re-render test (1).</td></tr>

<tr>
<td><strong>F-A21-04/05/06/07</strong></td>
<td>POS offline UX absent (white screen); no web manifest; idempotency client never sends key; tip collected but never persisted (POLISH_PLAN §3.9 "tip SHIPPED" also false)</td>
<td>
A21 report — full sweep of POS/PWA surface; backend idempotency exists, client omits; tip parsed at <code>pos.js:34</code> (TODO) but no <code>tip_amount</code> column; tip appears in UI + echoed + displayed, never written
</td>
<td>POS has no offline capability; duplicate-order risk in flaky networks; tips silently lost</td>
<td>Wave 2f (tip migration — owner pre-decided YES Q4); Wave 3f (idempotency client key); PWA build-or-demote under Q9</td>
<td>A21, A8, A4 (F-A4-5)</td>
</tr>

<tr>
<td><strong>F-A14-3</strong></td>
<td><code>env.AI</code> and <code>env.STATE_DO</code> used in code but absent from wrangler.toml</td>
<td><code>ai.js:560-608</code> — graceful 503 when binding absent; no <code>[ai]</code>/StateDO binding in wrangler.toml</td>
<td>AI server endpoints honest 503s — intended, but bindings ambiguity remains</td>
<td>Add wrangler.toml comments noting opt-in / not deployed; keep 503 stubs honest</td>
<td>A14</td>
</tr>
</table>

---

### P2 — Medium (functional gaps, data accuracy, tenant-integrity hardening)

<table>
<tr><th>ID</th><th>Title</th><th>Evidence</th><th>Fix direction</th><th>Cross-refs</th></tr>

<tr><td><strong>F-A4-2</strong></td><td>Reservation double-count: meal-plan total added to DB twice (INSERT + UPDATE) while Paymob charges once</td><td><code>reservations.js:299</code> — <code>effectiveTotal += mealPlanTotal</code> (INSERT :352 + Paymob :465); <code>:428-431</code> — UPDATE re-adds mealPlanTotal. DB = room + 2×meals; Paymob = room + meals</td><td>Remove the UPDATE at :428-431; add integration test (G-A10-2); owner confirms Q2</td><td>A4, A10 (G-A10-2)</td></tr>

<tr><td><strong>F-A14-4</strong></td><td>No CI/CD workflows — zero automated gates</td><td><code>.github/workflows/</code> does not exist; all suites documented but never run automatically</td><td>Add GitHub Actions: lint + unit tests + typecheck on PR; optional deploy on main</td><td>A14</td></tr>

<tr><td><strong>F-A17-01</strong></td><td>R2: no object cleanup on delete — every media delete leaks bytes</td><td>Media delete handlers remove DB rows only; R2 <code>MEDIA_BUCKET</code> objects never purged</td><td>Delete R2 object on media DELETE (best-effort, key-scoped)</td><td>A17 — <b>DONE, Wave 3.6a</b>: <code>DELETE /api/media/*</code> added (key-scoped, best-effort R2 purge) in this commit</td></tr>

<tr><td><strong>F-A17-02</strong></td><td>R2: import media PUT failure doesn't roll back — phantom provisioning (items 1–N inserted, media N+1 fails)</td><td>Tenant-import handler uploads base64→R2 mid-flow; no compensating deletes on later failure</td><td>Transactional rollback of imported rows on any R2 failure, or two-phase (upload-then-insert with cleanup)</td><td>A17, T1–T5</td></tr>

<tr><td><strong>F-A22-02</strong></td><td>Anonymous cross-tenant catalog reads via <code>x-tenant-id</code> header pivot</td><td>Public GET storefront endpoints accept arbitrary <code>x-tenant-id</code> and return that tenant's catalog without auth (intentional public scope, but header pivots across tenants)</td><td>Verify this is intended (public marketplace reads); if not, bind public reads to hostname-derived tenant only</td><td>A22 (runtime probe), A7</td></tr>

<tr><td><strong>F-A16-03/04 ⇧ → <span style="color:#1b5e20">DONE — Wave 3.4a/3.4b/3.4c</span></strong></td><td colspan="4"><b>2026-09-19: FIXED across Wave 3.4a–3.4c.</b> <b>3.4a</b> shipped the short-lived SSE-only token mint (<code>POST /api/stream/token</code>, 60s single-use jti burned via DO storage) so the 24h admin JWT never lands in a query string. <b>3.4b</b> adds the bounded replay buffer (<code>MAX_REPLAY_EVENTS = 100</code> + <code>MAX_REPLAY_BYTES = 128 * 1024</code>, drop-oldest) with an <code>id:</code> on every frame, an <code>event: reset</code> frame when the marker predates the buffer (client refetches rather than applying a partial replay), and <b>append → fan-out, fail-closed</b> ordering so a subscriber can never see a frame the replay buffer rejected. The crux: <code>broadcast()</code> previously early-returned <code>delivered: 0</code> with <i>zero retention</i> when no subscriber was live — exactly the offline-client case replay must serve. <b>3.4c</b> resets the reconnect attempt counter on a successful open so backoff restarts at 3s instead of resuming mid-curve. Regression tests: +12 backend (zero-subscriber retention, in-buffer replay, stale→reset, unknown marker, empty-buffer marker, count cap, byte cap, fail-closed append, per-tenant isolation) and +13 frontend (marker advances on receipt incl. deduped frames, reset listener + removal on close, reconnect carries the advanced marker, backoff resets after open). <b>F-A16-04 → DONE — Wave 3.4c, closed 2026-09-20</b>: the counter-reset code shipped bundled inside <code>2afb889</code> (one-commit-per-sub-wave deviation, accepted + recorded in the commit body <code>=== SCOPE NOTE ===</code>); formal DONE row + Wave 3 status table landed via commit <code>8f5ee72d251917781a1ee1e580734b6f0784a96c</code> (logbook/plan-only commit, no code change).</td></tr>

<tr><td><strong>F-A20-01</strong></td><td><code>PERF_BASELINE.md</code> is 4.2× stale — claims 501 KiB, actual 2112 KiB total JS</td><td>A20 measured current bundle; doc predates storefront islands + admin panels</td><td>Re-run measurement, update baseline, add "measure + update on major islands change" note</td><td>A20</td></tr>

<tr><td><strong>F-A20-02</strong></td><td>"Only 4 public islands" constraint already violated — 23 <code>client:*</code> sites incl. 4 storefront islands <code>client:load</code></td><td>A20 grep of <code>app/src</code>; AGENTS.md T15 discipline stale</td><td>Storefront islands → <code>client:visible</code> below-fold; re-document the discipline with the real count</td><td>A20</td></tr>

<tr><td><strong>F-A19-01..09</strong></td><td>Accessibility: 9× P2 across BookingCalendar drawer (no focus mgmt), InboxPanel tabs (no keyboard pattern), Select.tsx (no aria-activedescendant), ShopCatalog search/category (unlabeled), ShopCatalog toast (unannounced), StorefrontCart qty buttons (bare glyphs), gray-400/stone-400/placeholder contrast fails, modals lack aria-modal</td><td>A19 report (15 total; 9 P2 + 6 P3; zero P0/P1)</td><td>Batch a11y fixes per surface (Admin / POS / Storefront) — Wave 7 (does not block launch)</td><td>A19</td></tr>

<tr><td><strong>F-A3-2 / A1-F002</strong></td><td>Check-in order lookup: <code>camp_id</code> derived from request body rather than tenant-verified room</td><td>Part of the A22-01 blast radius; A1 also flagged <code>orders.js:238</code> (camp_id from body trusted)</td><td>Derive <code>camp_id</code> from the tenant-verified room row (same wave as 3a)</td><td>A3, A1, A22</td></tr>
<tr><td><strong>F-A3-2 ⇧ → <span style="color:#b00020">WITHDRAWN — false positive</span></strong></td><td colspan="4"><b>2026-09-19: WITHDRAWN in Wave 3.2.</b> The check-in handler destructures only <code>{ early_checkin, adult_count, child_count, room_id }</code> (orders.js:946-949) and the orders UPDATE never includes <code>camp_id</code> — the value is derived from the tenant-verified room row (A22-01 ownership JOIN at orders.js:979-989). Verified against pre-A22 code at <code>0d1975c^</code>: the handler never destructured <code>camp_id</code> even before the fix. One regression guard test retained (asserts no <code>camp_id</code> in the orders UPDATE, smuggled camp_id ignored, response <code>roomId</code>).</td></tr>

<tr><td><strong>F-A1-F001</strong></td><td><code>pos-users.js:196/255</code> — email/username duplicate check not scoped by <code>organization_id</code></td><td>A1 db audit: cross-org username collision possible in POS users</td><td>Scope duplicate check by org (backend fix)</td><td>A1</td></tr>

<tr><td><strong>F-A1-F004</strong></td><td>Duplicate-index bloat: 23 redundant indexes (legacy <code>idx_*</code> twins)</td><td>A1 verified against live D1 pragmas; every drop IF EXISTS with surviving twin checked</td><td>Proposed migration <code>0100_cleanup_redundant_indexes.sql</code> (reverted to proposed-only; apply in Wave 4)</td><td>A1</td></tr>
</table>

### P3 — Low (tech debt, docs, minor gaps)

<table>
<tr><th>ID</th><th>Title</th><th>Fix direction</th></tr>

<tr><td>F-A4-5 / F-A21-07</td><td>POS tip parsed but never persisted to <code>pos_transactions</code> (no column; TODO at pos.js:34) — owner pre-decided YES (Q4)</td><td>Migration <code>0102_tip_amount.sql</code> (proposed) + persist in INSERT + shift reports include tip</td></tr>
<tr><td>F-A4-6</td><td>No void or refund endpoints; ledger <code>refunded</code>/<code>voided</code> states dead</td><td>Owner pre-decided: defer (Q8). Track as next-cycle scope</td></tr>
<tr><td>A9-D1</td><td><code>PublicPayment.id</code> documented <code>number</code> (registry.js:2365), wire value is TEXT hex — generated <code>api-types.ts</code> consumers break on equality</td><td>Fix registry schema → <code>z.string()</code>; regen api-types.ts (deterministic)</td></tr>
<tr><td>A9-D2</td><td><code>MarketplacePayout.tenantId</code> documented <code>number</code> (registry.js:2420), wire/DB TEXT — sibling PublicPayment.tenantId already string</td><td>Fix registry schema → <code>z.string()</code>; regen</td></tr>
<tr><td>A9-D6</td><td><code>posUserSchema.isActive</code> documented <code>boolean</code>, wire is SQLite <code>0|1</code>; validator itself hedges with z.union</td><td>Adopt z.union([z.boolean(), z.union 0|1]) in schema; normalize in handler or document 0|1</td></tr>
<tr><td>A9-D4/D5</td><td>Cancel response omits required <code>items</code>; <code>itemCount</code> missing on detail/pay/cancel</td><td>Make detail/pay/cancel consistent with list schema (roll with Wave 2c)</td></tr>
<tr><td>F-A17-03/04</td><td>Import media: malformed base64 500s whole import; no magic-byte validation</td><td>Per-item validation errors instead of whole-import 500; magic-byte check before upload</td></tr>
<tr><td>F-A17-07</td><td>Health check reports false "missing bucket" when R2 binding errors; docs claim manual creation (stale)</td><td>Fix health probe; update wrangler.toml comment to "bucket exists; automation added for new envs"</td></tr>
<tr><td>F-A18-07</td><td>CORS doc inaccurate (<code>credentials: true</code> claim false; code actually correct single-source)</td><td>Fix security-guide.md CORS section to match code</td></tr>
<tr><td>F-A18-09</td><td>Rate limiter per-IP+path, not per-tenant — <b>REAL</b>, fixed Wave 3.5 (tenant-aware key on verified scope, Shape 1 post-auth mount). Sub-claim "5 public endpoints with no rate limit" — <b>WITHDRAWN, false positive</b> (zero unlimited public endpoints)</td><td>Wave 3.5 overlay limiter keyed <code>t:&lt;ip&gt;:&lt;tenantId&gt;:&lt;path&gt;</code>; public per-group budgets deferred to Wave 3.5b (Paymob webhook P1: shared 100/min IP bucket would drop payment callbacks)</td></tr>
<tr><td>F-A15-1</td><td>69 dead exports in <code>app/src/lib/api.ts</code> (24% of 291) — **confirmed by A9 regenerated consumer graph; stays P3** (owner asked to re-check for P2; A9 found no dynamic/string-key dispatch → P3)</td><td>Prune or auto-generate from consumer graph (Wave 5a)</td></tr>
<tr><td>F-A15-2</td><td>8 dead exports in <code>sharedAuth.js</code></td><td>Delete dead surface (Wave 5b)</td></tr>
<tr><td>F-A15-3</td><td><code>softDelete.js</code> (213 lines) wholly dead file, zero imports</td><td>Delete + its test (Wave 5c)</td></tr>
<tr><td>F-A15-4</td><td>21 dead imports across backend (4) + frontend (17)</td><td>Remove imports (Wave 5d)</td></tr>
<tr><td>F-A15-5</td><td>6 duplication clusters (<code>escapeUrl</code> ×5, <code>sanitizeForJsonLd</code> ×3, <code>normalizeAssetUrl</code> ×3, <code>escHtml</code> ×4, <code>slugify</code> ×2)</td><td>Centralize into <code>utils.ts</code>; note <code>is:inline</code> copies are required</td></tr>
<tr><td>F-A12-1..9</td><td>Doc drift across README, project-context, MIGRATION_GUIDE (P0: "next 0054" collision), TESTING, QUICK_START (Pages), API_SURFACE (Stripe Mock), POLISH_PLAN, DEEP_AUDIT, DATABASE_SCHEMA_AUDIT</td><td>Update all in Wave 6</td></tr>
<tr><td>F-A14-1</td><td>QUICK_START.md claims "Pages frontend" — actual is Workers</td><td>Update line 64/68 (Wave 6)</td></tr>
<tr><td>F-A14-5</td><td><code>deploy.sh</code> has zero rollback guidance</td><td>Add <code>--rollback</code> flag or document procedure (Wave 4)</td></tr>
<tr><td>F-A14-7</td><td>Root <code>package.json</code> has no <code>lint</code> or <code>typecheck</code> script</td><td>Add root scripts delegating to sub-packages (Wave 4)</td></tr>
<tr><td>F-A14-8</td><td>Staging frontend Worker has no explicit routes</td><td>Add <code>[[env.staging.routes]]</code> or document intent</td></tr>
<tr><td>F-A10-2</td><td>Doc claims 262 integration tests; actual 255</td><td>Update doc count (Wave 6)</td></tr>
<tr><td>F-A10-3</td><td>Frontend has no coverage threshold enforcement</td><td>Add vitest coverage config with conservative thresholds (owner pre-decided YES Q6)</td></tr>
<tr><td>F-A18-03</td><td>Migration 0076 documented as "XSS scrub layer" but is a weak one-time data scrub</td><td>Reclassify in security-guide.md (do not present as ongoing defense)</td></tr>
<tr><td>A9-parity</td><td>No CI check enforces api.ts ↔ api-types.ts ↔ OpenAPI parity</td><td>Add generation-diff check to CI (Wave 4c)</td></tr>
<tr><td>F-A21-08</td><td>POS tender methods: <code>ewallet</code>/<code>instapay</code> in docs but never wired</td><td>Remove from docs or implement (owner decision in Q9 bundle or next cycle)</td></tr>
</table>

### P4 — Info (design notes, low-risk)

<table>
<tr><th>ID</th><th>Title</th><th>Notes</th></tr>

<tr><td>F-A4-7</td><td>Payout "pay" just flips status; no external transfer</td><td>Owner pre-decided: keep as bookkeeping-only (Q3). Document in admin panel</td></tr>
<tr><td>F-A4-8</td><td>Webhook resolves order by reference only (no tenant scope at lookup)</td><td>Write is scoped correctly; add tenant_id to SELECT for defense-in-depth</td></tr>
<tr><td>F-A14-6</td><td><code>.env.example</code> documents Stripe vars never used in code</td><td>Remove or annotate "(not implemented)"</td></tr>
<tr><td>F-A14-9</td><td><code>app/.env.example</code> lists JWT_SECRET (frontend never reads it)</td><td>Remove or add note</td></tr>
<tr><td>F-A14-10</td><td>D1 backup not validated for non-empty after export</td><td>Add file-size check post-backup</td></tr>
<tr><td>A18-09b</td><td>POS merchant key stored in D1 (not KV)</td><td>Consider KV/secret move for POS Paymob keys (defense-in-depth)</td></tr>
<tr><td>A16-07</td><td>DO write buffer unbounded</td><td>Cap buffer; evict oldest</td></tr>
<tr><td>F-A22-03</td><td>Super-admin <code>?tenantId=</code> override ignored on public GETs</td><td>Documented; public GETs are intentionally tenant-from-header/hostname</td></tr>
<tr><td>F-A1-F003</td><td><code>rooms_new.tenant_id</code> NULL for all 161 rows (backfill proposed as migration 0101)</td><td>Proposed migration <code>0101_backfill_rooms_new_tenant_id.sql</code> — UPDATE-only, verified 100% match to projects.tenant_id; no query filters that column directly so behavior-neutral</td></tr>
<tr><td>A20-04</td><td>Browser-AI lazy-safe; ONNX stays out of main bundle (verified)</td><td>Keep; no change needed</td></tr>
</table>

**P5 (4):** A16-05 scopeMode lenient (allow no-tenantId admin tokens) — tighten if SSE grows; A16-09 no Broadcaster unit tests — add when SSE features land; A20-05 raw `img` no `loading=lazy` in ShopCatalog — include in Wave 7 perf sweep; A20-03 lazy giants (503.7+344.3+194.9 KiB on-demand admin) — Wave 7.

---

---

## Wave 3 — Execution Status (through 3.5)

| Item | Status | Commit |
|------|--------|--------|
| 3.1 pos-users org scope | ✅ | `79bb584` |
| 3.2 camp_id from room row | ✅ | `24d3b86` |
| 3.3 POS refresh + auth change | ✅ | `5609e3c` |
| 3.4a stream token | ✅ | `ebaa60d` |
| 3.4b bounded replay | ✅ | `2afb889` |
| 3.4c counter reset | ✅ | `8f5ee72d251917781a1ee1e580734b6f0784a96c` |
| 3.5 per-tenant limiter overlay (F-A18-09) | ✅ | this commit |
| 3.5b public endpoint budgets (marketplace, onboarding, availability, media, meal-plans, Paymob webhook) | held | — |
| 3.6a R2 object purge on media DELETE (F-A17-01) | ✅ | this commit |
| 3.6b import R2 put rollback (F-A17-02) | held | — |

Gate: 3.5 ✅, 3.6a ✅ → next 3.6b (import R2 put rollback, F-A17-02), then 3.5b (public per-group budgets), then Wave 3 report (C1/C2). 3.5b (public per-group budgets) carries a **P1 note**: the Paymob payment webhook shares the 100/min IP bucket — a busy NAT'd tenant IP would drop payment callbacks; give the webhook a dedicated/exempt budget in 3.5b. Deploy remains held (Wave 6.5 AND 6.6).

## Wave 6.6 — Human Testing Pre-Flight

**Purpose:** prove the three human-facing surfaces work end-to-end on staging with real accounts, real logins, and a real feedback loop — the last claim class that automated tests cannot settle. **Deploy is held until Wave 6.5 AND Wave 6.6 both pass.**

**Substrate (verified real — the first plan claim to survive direct inspection after three false positives):**
- `scripts/seed-test-users.js` (5954 B) — idempotent (a 409 is treated as success), env-overridable via `API_BASE_URL`, `TENANT_ID`, `TEST_ADMIN_PASSWORD`, `TEST_POS_PASSWORD`. Creates `admin.test@acaciacamp.com` / `TestPass123!` and `pos.test@acaciacamp.com` / `pass1234` (username `testpos`), logging in as super admin first.
- `backend/migrations/0029*.sql:51` seeds the super admin `admin@sinaicamps.com` (bcrypt `$2b$10$qRNP…`, role `super_admin`, `tenant_id = NULL`, comment `-- Password: sinairoot`).
- `app/src/components/debug/DebugFeedbackWidget.tsx` (widget) + `app/src/components/admin/FeedbackPanel.tsx` (owner panel).
- 30-day debug cookie: `app/src/layouts/PublicLayout.astro:784` sets `sc_debug=1; path=/; max-age=2592000; SameSite=Lax`; `:62` gates the widget on `?debug=1` **or** the cookie.

### 6.6.1 — Seed the staging accounts
Run `node scripts/seed-test-users.js` against the staging API base.
**Done when:** the script exits 0 and reports both test accounts; a second run is safe (idempotent 409 path).

### 6.6.2 — Prove the three logins live
Log in as `admin@sinaicamps.com` (super admin), `admin.test@acaciacamp.com` (tenant admin), and `pos.test@acaciacamp.com` (POS).
**Done when:** all three reach their intended shell on staging with no 401/403 — not merely "the login form renders".

### 6.6.3 — Exercise the debug feedback loop on all three surfaces
Submit one Feedback report from each surface: a public page with `?debug=1`, the admin SPA, and POS. Each submission must carry a screenshot (base64 → R2 → `/api/media/` URL).
**Done when:** 3 submissions succeed and 3 rows exist server-side.

### 6.6.4 — Owner verification + staging checklist (DEPLOY GATE)
The owner confirms all five, on staging, before deploy:
1. `node scripts/seed-test-users.js` ran green against staging.
2. All 3 accounts logged in (§6.6.2).
3. 3 Feedback reports submitted, one per surface (§6.6.3).
4. The 3 reports appear in the owner's Feedback panel **with screenshots rendered**.
5. The 3 test reports are deleted/archived.

**Three behavioral gaps this checklist exists to close** — file-presence was verified in the pre-reads; *behavior* was not:

| # | Gap | Why file-presence is not enough |
|---|-----|---------------------------------|
| 1 | **Super-admin tenant binding** | Login passes `tenantId: 'marketplace'`, while `0029` seeds `tenant_id = NULL`. Whether login succeeds — and with which tenant scope — can only be proven by an actual login on staging. |
| 2 | **Live login** | "Accounts can log in" is only true after `scripts/seed-test-users.js` actually runs against staging; the seed and the login must both be exercised live, not assumed. |
| 3 | **Screenshot + panel lifecycle** | `DebugFeedbackWidget.tsx` and `FeedbackPanel.tsx` exist, but base64→R2 embedding, panel render/full-size view, and the `open → in progress → resolved` transition are unverified behavior. |